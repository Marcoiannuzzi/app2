import { EstiloTitulosDirective } from './estilo-titulos.directive';

describe('EstiloTitulosDirective', () => {
  it('should create an instance', () => {
    const directive = new EstiloTitulosDirective();
    expect(directive).toBeTruthy();
  });
});
