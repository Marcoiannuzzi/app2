import { AgregarApellidoPipe } from './agregar-apellido.pipe';

describe('AgregarApellidoPipe', () => {
  it('create an instance', () => {
    const pipe = new AgregarApellidoPipe();
    expect(pipe).toBeTruthy();
  });
});
